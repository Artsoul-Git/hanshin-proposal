{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"import sys,json; d=json.load(sys.stdin); cmd=d.get('tool_input',{}).get('command',''); exit(0 if 'git commit' in cmd else 99)\" && git push 2>/dev/null || true",
            "shell": "bash",
            "statusMessage": "Auto-pushing to remote...",
            "timeout": 30
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "git add -A && git diff --cached --quiet || git commit -m \"auto: $(date '+%Y%m%d_%H%M%S') 自動保存\"",
            "shell": "bash",
            "statusMessage": "自動保存中...",
            "timeout": 30
          }
        ]
      }
    ]
  }
}
